apiVersion: extensions/v1beta1
kind: ReplicaSet
metadata:
  namespace: sqlfiddle
  name: mysql56
spec:
  replicas: 1
  template:
    metadata:
      labels:
        type: mysql56
    spec:
      containers:
        - image: {{ .Values.registryPrefix }}/sqlfiddle:mysql56Host
          imagePullPolicy: IfNotPresent
          name: mysql56
          ports:
            - containerPort: 3306
              protocol: TCP
          resources:
              requests:
                memory: "512Mi"
          readinessProbe:
            exec:
              command:
                - "/ready.sh"
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
---
kind: Service
apiVersion: v1
metadata:
  namespace: sqlfiddle
  name: mysql56-service
  labels:
    role: host
spec:
  clusterIP: None
  ports:
    - port: 3306
      name: database
  selector:
    type: mysql56
